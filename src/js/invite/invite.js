import Invite from '../../components/invite/invite.vue';
import '../../js/lib/layer.js';
import '../../js/lib/layer.css';

var V = new Vue({
    el: '#container',
    components: {
        Invite
    },
});
